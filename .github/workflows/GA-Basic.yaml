name: npm test

on: 
  workflow_dispatch:
               
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      name : checkOutCode
      

          # TO COMPLETE

#$GITHUB_WORKSPACE